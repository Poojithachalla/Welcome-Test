# This workflow will build a Java project with Maven and cache/restore dependencies to improve workflow execution time
# For more information, see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

name: Java and Git Maven

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn -B package --file pom.xml

      - name: Build & push Docker image
        uses: mr-smithers-excellent/docker-build-push@v5
        with:
          image: challapoojitha/springboot-githubaction
          tags: latest
          registry: docker
<<<<<<< HEAD
          username: challapoojitha
=======
          username: poojitha22.ch@gmail.com
>>>>>>> 6af618db904729339225e74a9b769bb82c0c73ff
          password: Myworld@1

      # Optional: Upload the full dependency graph to GitHub
      - name: Update dependency graph
        uses: advanced-security/maven-dependency-submission-action@v1
